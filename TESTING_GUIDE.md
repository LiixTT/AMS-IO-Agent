# 测试指南

## ✅ 测试命令

可以使用以下命令运行测试：

```bash
python tests/test_io_ring_tool.py
```

## 📋 测试内容

测试脚本会测试以下功能：

1. **列出配置文件** - 测试 `list_intent_graphs()`
2. **配置验证** - 测试 `validate_intent_graph()` 对28nm和180nm配置的验证
3. **原理图生成** - 测试 `generate_io_ring_schematic()` 对28nm和180nm的原理图生成
4. **版图生成** - 测试 `generate_io_ring_layout()` 对28nm和180nm的版图生成

## 🔧 前置条件

### 1. 安装依赖

```bash
# 激活虚拟环境（如果使用）
source .venv/bin/activate  # 或 source venv/bin/activate

# 安装依赖
pip install -r requirements.txt
# 或使用 uv
uv pip install -r requirements.txt
```

### 2. 确保测试文件存在

测试需要以下配置文件：
- `output/example_T28/intent_graph.json` - 28nm配置
- `output/example_T180/intent_graph.json` - 180nm配置

## 🎯 测试输出

测试会输出：
- ✅ 成功信息
- 📊 统计信息（设备数量、类型等）
- ⚠️ 警告信息（如果模板未找到等）
- ❌ 错误信息（如果有问题）

## 🔍 验证解耦架构

测试会验证：
1. 工厂模式是否正确创建28nm和180nm生成器
2. 每个工艺节点的生成器是否独立工作
3. 生成的SKILL代码是否正确

---

**现在可以运行 `python tests/test_io_ring_tool.py` 进行测试！** 🎉

