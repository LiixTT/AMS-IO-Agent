# 字段名统一总结

## ✅ 已完成的统一工作

### 1. 配置文件字段统一

已将180nm配置文件 (`output/example_T180/intent_graph.json`) 的字段名统一为28nm格式：

#### 字段名转换
- ✅ `device_type` → `device`
- ✅ `pin_config` → `pin_connection`
- ✅ 添加 `width` 和 `height` 字段到 `ring_config`
- ✅ 添加 `process_node: "180nm"` 到 `ring_config`

#### 保留的180nm特有字段（作为可选字段）
- `view_name`: 视图名称（可选）
- `domain`: 域信息（可选）
- `pad_width`: Pad宽度（可选，会覆盖默认值）
- `pad_height`: Pad高度（可选，会覆盖默认值）
- `chip_width`: 芯片宽度（可选）
- `chip_height`: 芯片高度（可选）
- `top_count`, `bottom_count`, `left_count`, `right_count`: 各边pad数量（可选）

### 2. 代码简化

#### Layout Generator
- ✅ 移除了 `device_type` 兼容代码
- ✅ 移除了 `pin_config` 兼容代码
- ✅ 统一使用 `device` 和 `pin_connection` 字段
- ✅ 保留对可选字段的支持（view_name, domain等）

#### JSON Validator
- ✅ 支持从 `top_count/bottom_count/left_count/right_count` 推导 `width/height`
- ✅ 保持对 `device` 字段的验证（向后兼容 `device_type`）

### 3. 统一后的格式

#### 标准格式（28nm和180nm通用）
```json
{
    "ring_config": {
        "width": 3,                    // 必需：top/bottom边pad数量
        "height": 3,                   // 必需：left/right边pad数量
        "placement_order": "clockwise", // 必需
        "process_node": "180nm",       // 可选：指定工艺节点
        // 180nm可选字段
        "chip_width": 2250,
        "chip_height": 2160,
        "pad_spacing": 90,
        "pad_width": 80,
        "pad_height": 120,
        "corner_size": 130,
        "top_count": 3,
        "bottom_count": 3,
        "left_count": 3,
        "right_count": 3
    },
    "instances": [
        {
            "name": "VIOLA",
            "device": "PVDD1CDG",      // 统一字段名
            "position": "top_0",
            "type": "pad",
            "pin_connection": {        // 统一字段名
                "VDD": {"label": "VIOLA"}
            },
            // 180nm可选字段
            "view_name": "layout",
            "domain": "analog",
            "pad_width": 80,
            "pad_height": 120
        }
    ]
}
```

## 📋 字段对照表

| 功能 | 28nm格式 | 180nm格式（已统一） | 状态 |
|------|---------|-------------------|------|
| 设备类型 | `device` | ~~`device_type`~~ → `device` | ✅ 已统一 |
| Pin连接 | `pin_connection` | ~~`pin_config`~~ → `pin_connection` | ✅ 已统一 |
| Ring尺寸 | `width`, `height` | `width`, `height` (新增) | ✅ 已统一 |
| 工艺节点 | 默认28nm | `process_node: "180nm"` | ✅ 已支持 |

## 🎯 优势

1. **统一格式**：两种工艺节点使用相同的字段名
2. **代码简化**：移除了兼容层代码，代码更清晰
3. **向后兼容**：保留对可选字段的支持
4. **易于维护**：统一的格式便于未来扩展

## ⚠️ 注意事项

1. **必需字段**：所有配置文件必须包含 `device` 和 `pin_connection` 字段
2. **可选字段**：180nm特有的字段（如 `view_name`, `domain`）可以保留，但不会影响核心功能
3. **工艺节点**：通过 `process_node` 字段或工具参数指定

---

**统一完成！现在两种工艺节点使用完全相同的字段格式！** 🎉

